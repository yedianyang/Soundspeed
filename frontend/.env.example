# 后端 API 地址（含协议 + host + port，不含尾部斜杠）。
# WS_BASE 由前端从此值派生：http→ws / https→wss。
VITE_API_BASE=http://localhost:8000

# dev token 自动填默认值。仅 dev 构建（import.meta.env.DEV）生效，无 localStorage token 时用它自动连接。
# 留空则 dev 默认用 "devtoken"（后端 SOUNDSPEED_DEV 用固定 "devtoken"）。生产构建忽略此项，仍需手填 token。
VITE_ADMIN_TOKEN=devtoken
