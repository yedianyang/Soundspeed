# Soundspeed 局域网 HTTPS 入口（给手机发语音 / 图片 / 文件）。
#
# 为什么要它：手机录音用 getUserMedia，浏览器只在 secure context（HTTPS）下放行麦克风。
# 局域网 IP 走 HTTP 拿不到麦克风。图片/文件本来 HTTP 就能传，HTTPS 只为语音。
#
# 思路：Caddy 占 443 做唯一 HTTPS 终止点，前后端两个 HTTP 服务原封不动。
#   /api/* /ws /healthz  → 转发后端 :8000（WebSocket 的 Upgrade 自动透传）
#   其余                 → 出前端静态产物 frontend/dist（SPA，404 回 index.html）
#
# 跑法（从仓库根目录）：
#   1. cd frontend && pnpm build         # 出 frontend/dist（用 .env.production 的 HTTPS host）
#   2. 后端照常起，监听 0.0.0.0:8000
#   3. sudo caddy run --config ./Caddyfile   # 443 是特权端口，要 sudo
#   4. 手机同 Wi-Fi 开 https://macbook-pro-1370.local/admin
#
# 证书：cert/key 在 ./certs（gitignore），由 mkcert 生成、SAN 含 .local 主机名 + 局域网 IP。
# 手机要先信任 mkcert 根 CA，否则 wss 握手失败。详见 docs 里的 runbook。
#
# 换机器：改下面站点地址的 hostname/IP，并按 SAN 重新生成 ./certs。

macbook-pro-1370.local, 192.168.0.190 {
	tls ./certs/soundspeed.pem ./certs/soundspeed-key.pem

	@backend path /api/* /ws /healthz
	reverse_proxy @backend localhost:8000

	root * ./frontend/dist
	file_server
	try_files {path} /index.html
}
